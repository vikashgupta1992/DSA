// Create a 2D array with given number in parameter and array should be in spiral order

const generateSpiralArray = (count) => {
    if(count < 1) return [];

    const m = 5;
    const n = 5;
    let fieldCount = 0;
    const result = [];

    while (fieldCount < m * n) {
        //Top Scope
            

        //right Scope

        //Botton Scope
    }

}

const result = generateSpiralArray(5);
console.log(result);